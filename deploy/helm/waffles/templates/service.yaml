apiVersion: v1
kind: Service
metadata:
  name: {{ include "waffles.fullname" . }}
  labels:
    {{- include "waffles.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.config.metrics.enabled }}
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "waffles.selectorLabels" . | nindent 4 }}
